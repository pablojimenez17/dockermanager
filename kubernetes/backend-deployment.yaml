apiVersion: apps/v1
kind: Deployment
metadata:
  name: dockermanager-backend
  namespace: dockermanager
  labels:
    app: dockermanager-backend
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0   # Nunca baja una réplica antes de que la nueva esté lista
  selector:
    matchLabels:
      app: dockermanager-backend
  template:
    metadata:
      labels:
        app: dockermanager-backend
    spec:
      imagePullSecrets:
        - name: ghcr-credentials
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - dockermanager-backend
              topologyKey: "kubernetes.io/hostname"
      containers:
      - name: backend
        image: ghcr.io/pablojimenez17/dockermanager-backend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
          name: http
        envFrom:
        - configMapRef:
            name: dockermanager-config
        env:
        - name: NODE_ENV
          value: "production"
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: dockermanager-secrets
              key: JWT_SECRET
        - name: GEMINI_API_KEY
          valueFrom:
            secretKeyRef:
              name: dockermanager-secrets
              key: GEMINI_API_KEY
        - name: SENDGRID_API_KEY
          valueFrom:
            secretKeyRef:
              name: dockermanager-secrets
              key: SENDGRID_API_KEY
        - name: MINIO_ROOT_USER
          valueFrom:
            secretKeyRef:
              name: dockermanager-secrets
              key: MINIO_ROOT_USER
        - name: MINIO_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: dockermanager-secrets
              key: MINIO_ROOT_PASSWORD
        - name: STRIPE_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: dockermanager-secrets
              key: STRIPE_SECRET_KEY
              optional: true
        - name: STRIPE_WEBHOOK_SECRET
          valueFrom:
            secretKeyRef:
              name: dockermanager-secrets
              key: STRIPE_WEBHOOK_SECRET
              optional: true
        resources:
          limits:
            cpu: "1"
            memory: 1Gi
          requests:
            cpu: 200m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /api/health
            port: 5000
          initialDelaySeconds: 45
          periodSeconds: 15
          timeoutSeconds: 5
          failureThreshold: 5
        readinessProbe:
          httpGet:
            path: /api/health
            port: 5000
          initialDelaySeconds: 15
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
