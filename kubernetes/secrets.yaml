apiVersion: v1
kind: Secret
metadata:
  name: dockermanager-secrets
  namespace: dockermanager
type: Opaque
data:
  # ATENCIÓN: Este archivo tiene valores placeholder.
  # Los valores reales se inyectan desde GitHub Secrets en el CI/CD.
  # NUNCA commitear secretos reales aquí.
  JWT_SECRET: CHANGE_ME_BASE64
  GEMINI_API_KEY: CHANGE_ME_BASE64
  SENDGRID_API_KEY: CHANGE_ME_BASE64
  MINIO_ROOT_USER: CHANGE_ME_BASE64
  MINIO_ROOT_PASSWORD: CHANGE_ME_BASE64
