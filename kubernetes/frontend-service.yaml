apiVersion: v1
kind: Service
metadata:
  name: dockermanager-frontend
  namespace: dockermanager
  labels:
    app: dockermanager-frontend
spec:
  ports:
  - port: 80
    targetPort: 80
    name: http
  selector:
    app: dockermanager-frontend
  type: ClusterIP
